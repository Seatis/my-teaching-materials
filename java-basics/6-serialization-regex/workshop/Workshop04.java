import java.util.Arrays;
import java.util.List;

public class Workshop04 {
    public static void main(String... args) throws IOException {
        List<HtmlTag> tags;

        // Import the Gson library
        // Deserialize the previously serialized tags.json using the Gson library
        // Use the Person class from the previous example
        // Print the size of "tags" to the standard output
        // Expected output: 2
    }
}
