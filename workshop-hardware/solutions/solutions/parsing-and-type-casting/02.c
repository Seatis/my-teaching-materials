#include<stdio.h>

int main()
{
	double x = 1.2;

    //TODO: write a program wich adds up x and 1, but our goal is to see 2 as sum.

    int sum = (int)x + 1;

    printf("sum = %d", sum);

    return 0;
}
