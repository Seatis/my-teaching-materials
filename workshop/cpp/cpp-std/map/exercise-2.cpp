#include <iostream>
#include <vector>

using namespace std;

// create a function that prints a vector using iterators

int main() {
  vector<int> v;
  for (int i = 0; i < 10; ++i) {
    v.push_back(i);
  }

  return 0;
}
