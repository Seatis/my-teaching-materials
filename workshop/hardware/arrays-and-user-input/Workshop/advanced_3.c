#include <stdio.h>

int main()
{
    int arr[20];
    int freq[20];

    // TODO: write a program wich asks for a number (under 20), that will be the size of the elemet, where real elemets will be loaded
    // Then enter elements in array
    // Finally print all unique elements in array


    return 0;
}
