#include <stdio.h>

int main()
{
    int arr[50];

    // TODO: write a program wich asks for a number, that will be the size of the array, with real elemets
    // Then enter elements in array
    // Then ask for a number, that will be the position, where you want the element to be deleted
    // please handle the requests for deleting from invalid positions
    // print out the refreshed array

    return 0;
}
