#include <stdio.h>
#include <stdlib.h>

int main()
{
    // TODO:
    // Please create a program that asks for a count when it starts.
    // It should ask for a number count times, then it shoud print the maximum of the numbers.

    return 0;
}
