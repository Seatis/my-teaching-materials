#include <stdio.h>
#include <stdlib.h>

// TODO:
// Write a function that takes a regular integer matrix as parameter (n*n matrix). The other parameter should be the n.
// The function should return 1 if in the diagonal line of the matrix is full of 0, else return 0.								
// So if we have a matrix like this:	[0][2]	the return value is 1.
//										[5][0]


int main() {

    return 0;
}