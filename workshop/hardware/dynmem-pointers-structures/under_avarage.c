#include <stdio.h>
#include <stdlib.h>

// TODO:
// Create a function that takes a double array as a parameter, and the length of array
// this function should calculate the avarage of the elements
// calculate how much elements are under the avarage
// allocate a dynamic array, and load into it, the elements that are under the avarage
// the return value should be the length of the new array

int under_avarage(double* array, int length, double* new_array);

int main() {

    return 0;
}