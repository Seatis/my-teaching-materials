# Create a "users" class that loads the users.csv and has the following methods:
#  - get_row <- takes an index of the row
#  - set_row <- takes an index and a data to set
#  - delete_row <- takes an index 
#  - find_by_field <- takes a field name or index and a search term
#
# Writing the file back is not needed